<p>タスク一覧</p>
<%= link_to "投稿画面へ", new_task_path %>
<% @tasks.each do |task| %>
  <p>
    <%= task.title %>
    <%= link_to "詳細", task_path(task), method: :get %>
    <%= link_to "編集", edit_task_path(task), method: :get %>
  </p>
<% end %>